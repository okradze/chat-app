type Query {
    users: [User!]!
}

type Mutation {
    signup(data: SignupInput!): User!
}

type User {
    _id: ID!
    fullName: String!
    email: String!
    password: String!
}

input SignupInput {
    firstName: String!
    lastName: String!
    email: String!
    password: String!
}
